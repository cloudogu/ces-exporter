apiVersion: v1
kind: Service
metadata:
  name: {{ include "ces-exporter.name" . }}
  labels:
    {{- include "ces-exporter.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http-port
  selector:
    app.kubernetes.io/name: ces-exporter